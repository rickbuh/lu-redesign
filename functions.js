// when open-nav is clicked pull in navbar from the right
function openNav() {
    document.getElementById("navbar").style.right = "0%";
}

// when close-nav is clicked reset navbar back off screen
function closeNav() {
    document.getElementById("navbar").style.right = "-50%";
}
